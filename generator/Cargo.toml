[package]
name = "generator"
version = "0.1.0"
edition = "2024"

[dependencies]
core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4.42", features = ["serde"] }